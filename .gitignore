# build output
dist/
.astro/

# dependencies
node_modules/

# pagefind
public/pagefind/

# netlify
.netlify/

# environment — the GitHub App private key lives here, never in git
.env
.env.*
!.env.example

# macOS
.DS_Store

# logs
*.log
npm-debug.log*

# impeccable local session cache — critique reports under .impeccable/critique/
# are kept and versioned; this is per-session edit bookkeeping only
.impeccable/hook.cache.json
