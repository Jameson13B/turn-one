{
  "name": "turn-one",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Turn One by Atomic10 — quick manuals for tabletop games",
  "license": "CC-BY-SA-4.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "check": "astro check",
    "validate": "node scripts/validate-content.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "pagefind": "^1.1.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.0"
  }
}
